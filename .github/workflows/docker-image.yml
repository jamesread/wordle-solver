name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - uses: actions/login-action@v3.1.0
      with:
        username: jamesread
        password: ${{ secrets.CONTAINER_TOKEN }}
      
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag ghcr.io/jamesread/wordle-solver:$(date +%s)

    - name: Docker push
      run: docker push ghcr.io/jamesread/wordle-solver
      
